apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: flask-login-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: flask-login
  namespaceSelector:
    matchNames:
      - default  # Replace with your app's namespace
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
